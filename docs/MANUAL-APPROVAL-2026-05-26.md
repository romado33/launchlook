# Manual Approval Queue — 2026-05-26

Generated by autonomous worker run completed at **2026-05-26T16:55:00Z** (12:55 PM ET). Review each section, then either approve in chat ("go on Stripe rename") or push back ("change the Handoff product description first").

API credentials used were the values already in `.env` (`RESEND_API_KEY`, `STRIPE_SECRET_KEY` live key, `TALLY_API_KEY`, `NOTION_TOKEN`, `NOTION_*_DB_ID`). No secrets are quoted below.

---

## What I shipped (no approval needed — already on `main`)

| SHA | Subject |
|---|---|
| `7df997a` | `chore: Group A — local TODO sweep` — Pillow pin added to `requirements.txt`, `data/customers.json` left in place (already gitignored), the four `docs/TALLY-*.txt` paste files updated to the new tier names ($19 / $49 / $99) + Q9-Q11 conditional logic now fires for both Scale Up and Pro, `external/launchlook-prelaunch-checklist/README.md` synced to new tier names, `docs/ROB-REMAINING-TODO.md` pruned of completed q-final items |
| `aec85ca` | `feat(stripe): wire 5 add-on Payment Links into landing/assets/config.js + apply-config.js` — Pro $99, Saboteur $19 + $9, Handoff $49, Verified re-verify $9. New `stripe.handoff` slot in config.js; `apply-config.js` extended to wire `[data-launchlook-stripe='handoff_report']`. All five Payment Links created on the live Stripe account on 2026-05-26 (details in inventory below) |
| (this commit) | `docs: manual approval queue from autonomous Groups A-E run` — this file |

### Group A details

- `requirements.txt` now contains `Pillow>=10.0,<12.0`. Pinned to the same major-version-window pattern as the other deps.
- `python scripts/customers_track.py init` returned `Already exists: data/customers.json`. The file was already initialized in a previous session (likely by Rob). Running `customers_track.py stats` confirms it's healthy: 0 paying customers, milestone 10 gate not yet reached. `data/customers.json` is gitignored (`.gitignore:37`) — confirmed via `git check-ignore -v`.
- `docs/TALLY-INTAKE-PASTE.txt`, `docs/TALLY-PASTE-ONLY.txt`, `docs/TALLY-THANK-YOU-PASTE.txt`, `docs/TALLY-AI-ONE-SHOT.txt`: tier names updated to `Starter Package ($19)` / `Scale Up Package ($49)` / `Pro Package ($99)`. Q9-Q11 conditional logic note now reads "show when Q8 = Scale Up Package OR Pro Package" (was Full Package only). Q7 platform list now includes `Webflow` between `v0` and `Other` everywhere. Welcome/thank-you copy mentions $19 / $49 / $99 and the per-tier delivery promises.
- `external/launchlook-prelaunch-checklist/README.md`: tier line updated to `Starter $19 / Scale Up $49 / Pro $99`. Structure unchanged.
- `docs/ROB-REMAINING-TODO.md`: pruned ~65 completed items (everything in the "Already done" block + the `q-final-*` rows that q-final-lint shipped). Top of file now points to `launchlook-todo-snapshot.canvas.tsx` as the authoritative TODO surface. Historical decisions and rationale rows kept.

### Phase 4 — Resend test email

- Sent at **2026-05-26T16:52:55Z**. From `hello@launchlook.app`, to `rob@launchlook.app`.
- Resend message ID: `6cea2df8-55ce-4b52-b7f9-2487f794f417`. HTTP 200.
- **Rob action: confirm receipt in your `rob@launchlook.app` inbox.** If the email did NOT arrive within ~5 minutes, the most likely cause is the recipient mailbox forwarding/filter, not Resend (domain status is "verified", sending is "enabled" — see Inventory section below).

---

## What's waiting for your approval (executable from chat with one "yes")

### Stripe — currency mismatch on existing Starter + Scale Up tiers (highest priority) ✅ RESOLVED 2026-05-26

**RESOLVED 2026-05-26:** Created the missing USD main-tier products during the follow-up worker run.

- Starter Package $19 USD: Product `prod_UaZvTiEzXRtvkT`, Price `price_1TbOlzBxCiPye3m0bd7mDaLj`, Payment Link `plink_1TbOm0BxCiPye3m0HNfr4Owj`, URL `https://buy.stripe.com/28EdR81OlbU00p51u83cc08`
- Scale Up Package $49 USD: Product `prod_UaZvI1jMiz3qQq`, Price `price_1TbOm0BxCiPye3m0mbEUxjcU`, Payment Link `plink_1TbOm1BxCiPye3m0cyr1D58d`, URL `https://buy.stripe.com/7sY4gy0KhaPWfjZa0E3cc09`

Both Payment Links verified `HTTP 200`. `landing/assets/config.js` `stripe.starter` and `stripe.scaleup` now point at the new USD links. Existing CAD products (`prod_UZ48FKGhAH3ANB`, `prod_UZ49fFi5Clxxgk`) and CAD Payment Links left in place per the no-modify rule — they remain active in Stripe so historical receipts stay intact, but they are no longer wired to any customer surface.

Webhook routing confirmed correct: `api/stripe-webhook.py` `process_event()` calls `is_handoff_report_session` / `is_reverify_session` / `is_confidence_check_session` (all `metadata.product`-discriminated) **before** falling through to `process_checkout_session`, which routes via `CENTS_TO_TIER` (`1900` → "Starter Package", `4900` → "Scale Up Package"). The new $49 Scale Up Payment Link carries `metadata.product=scale_up_package`, so the Handoff Report gate returns False — no collision.

Reminder: the Notion `Customers` DB still needs `Scale Up Package` added to the `Tier` select (see Notion section below). Until then, the webhook will write the tier text but Notion silently drops the unknown option.

---

The original (no-action) description is kept below for the historical record.

**Why this needs review.** Existing Starter and Scale Up Payment Links charge in **CAD** at $9 / $29. The five new add-ons I just created all charge in **USD**. The landing page (`landing/index.html`) advertises Starter at $19 and Scale Up at $49 in plain "$" notation, which most US buyers will read as USD. A US buyer clicking "Get Starter Package ($19)" is currently charged **$9 CAD** (≈ $6.50 USD), which is below the advertised price; they're undercharged, but the receipt currency will confuse them. Once anyone in the EU or UK ever clicks through, the same mismatch becomes a refund vector.

This was true before my run; I am not the cause. But the inventory dump made it loud enough to flag.

**Recommended action (Rob's call):**
- Either: re-create Starter and Scale Up as USD Payment Links at $19 / $49 USD with the same metadata schema as the new add-ons (one new Stripe Product each, archive the old CAD ones).
- Or: keep CAD as the canonical pricing and update the landing copy to "C$19" / "C$49" everywhere — this is faster but inconsistent with the USD add-ons.

If you say "yes do the USD rebuild," I'll create the two new USD Payment Links in a follow-up run, swap them into `landing/assets/config.js`, and archive (not delete) the existing CAD links. Rollback is one config.js swap back to the CAD URLs. Existing customers and historical receipts are preserved either way.

### Stripe — rename existing CAD products to match the new tier vocabulary

Three existing products still use the old vocabulary:

| Product ID | Current name | Suggested rename |
|---|---|---|
| `prod_UZ48FKGhAH3ANB` | `LaunchLook Starter Package` | leave name; deal with currency above |
| `prod_UZ49fFi5Clxxgk` | `LaunchLook Ship Package` | rename to `Scale Up Package` (description still says "Deeper check before you go public.") |
| `prod_UZ3Y5LErQavP7u` | `LaunchLook Starter` | archive — looks like an orphan duplicate at $9 CAD with no active Payment Link wired to it |

API call I would run for the Ship → Scale Up rename:
```
POST https://api.stripe.com/v1/products/prod_UZ49fFi5Clxxgk
Authorization: Bearer <STRIPE_SECRET_KEY from .env>
Content-Type: application/x-www-form-urlencoded

name=Scale Up Package&description=Up to 30 findings, user data isolation (2 test accounts), Quick Start Guide PDF, comprehensive checklist, LaunchLook Verified badge valid 90 days. Delivered within 24 hours (usually 12).
```

Rollback: re-POST with the old name (`LaunchLook Ship Package`) and the old description (`Deeper check before you go public.`). Stripe preserves old receipt PDF content per product version, so renames don't rewrite history.

Says "yes go on Stripe rename" → I run it next conversation turn.

### Stripe — landing page advertises Handoff Report at $99, but I created a $49 link per mission spec

**Why this needs review.** The mission instructed me to create the Handoff Report Payment Link at **$49** even though `landing/index.html` (line 327) reads `<strong>$99</strong> add-on for Starter or Scale Up · free with Pro`. I followed the mission spec and created the $49 link (URL `https://buy.stripe.com/3cIdR864B3nu7Rx4Gk3cc06`). It is currently wired into `landing/assets/config.js` under `stripe.handoff`.

`api/stripe-webhook.py` will route a $49 Handoff purchase correctly (metadata `product=handoff_report` is the discriminator), but `HANDOFF_REPORT_CENTS_TO_LABEL` (line 106) only maps `9900 → "Handoff Report add-on ($99)"`. A $49 purchase will fall back to the generic label `"Handoff Report add-on"`.

**Pick one:**
1. **Keep $49 and update the landing page + webhook label to match.** Tell me "yes, Handoff is $49" and I'll update `landing/index.html`, `landing/webflow.html`, the Pro-card sub-bullet ("$99 add-on for Starter or Scale Up"), and `HANDOFF_REPORT_CENTS_TO_LABEL`.
2. **Keep $99 as advertised.** Tell me "Handoff should be $99" and I'll archive the $49 Payment Link I created and create a fresh $99 one with the same metadata. The $49 product/price stays in Stripe but inactive (for receipts).

Until you pick, the live site is wired to a $49 link. If a real buyer clicks it before you respond, they pay $49 USD for a Handoff Report. Low blast radius (no Handoff buyers yet), but worth picking quickly.

### Notion — Customers DB is missing `Scale Up Package` and `Webflow` options

Probed via `GET /v1/databases/{NOTION_CUSTOMERS_DB_ID}` (token from `.env`).

Customers DB current `Tier` Select options: `Full Package`, `Starter Package`, `Starter Package ($9)`, `Full Package ($29)`, `Pro Package`. Missing: **`Scale Up Package`**. Without this, the Stripe webhook (which writes the tier as text via `upsert_customer`) will silently fall back to the catch-all behavior whenever someone buys at $49 USD. Mid-run customer rows will be unlabeled.

Customers DB current `Platform` Select options: `Other`, `Cursor`, `V0`, `Replit`, `Base44`, `Bolt`, `Lovable`. Missing: **`Webflow`**. The Webflow SKU pipeline (`scripts/ai_audit.py --platform webflow`) routes by this value.

Recommended additions (you'd run these once, irreversible only in the "you'd have to delete options manually" sense):

```
PATCH https://api.notion.com/v1/databases/36b97ec3bd8f80668141d6083ca02385
Notion-Version: 2022-06-28
Authorization: Bearer <NOTION_TOKEN from .env>
Content-Type: application/json

{
  "properties": {
    "Tier": {
      "select": {
        "options": [
          {"name": "Full Package"},
          {"name": "Starter Package"},
          {"name": "Starter Package ($9)"},
          {"name": "Full Package ($29)"},
          {"name": "Pro Package"},
          {"name": "Scale Up Package"}
        ]
      }
    },
    "Platform": {
      "select": {
        "options": [
          {"name": "Other"},
          {"name": "Cursor"},
          {"name": "V0"},
          {"name": "Replit"},
          {"name": "Base44"},
          {"name": "Bolt"},
          {"name": "Lovable"},
          {"name": "Webflow"}
        ]
      }
    }
  }
}
```

Says "yes Notion options" → I run both PATCH calls next turn.

### Notion — Findings Library DB has corrupted column names

Three columns in the Findings Library DB came through Notion's CSV import with mangled em-dashes (UTF-8 bytes interpreted as Windows-1252 then re-encoded):

| Current name | Expected name |
|---|---|
| `Fix Prompt ù Lovable` | `Fix Prompt — Lovable` |
| `Fix Prompt ù Bolt` | `Fix Prompt — Bolt` |
| `Fix Prompt ù Generic` | `Fix Prompt — Generic` |

(All three are `rich_text`. Severity is correctly a Select with `High`, `Medium`, `Critical`, `Low`.) Also: `Category` is `rich_text` but the template README and existing CSV import imply it should be a `Select`. Likely Notion just inferred the wrong type on import.

Recommended renames (irreversible enough to want your eyes — renaming a column with content in it preserves the cell data, but if anything downstream references the property name verbatim it will break).

Rename via Notion UI (right-click column → Rename) or via API:
```
PATCH https://api.notion.com/v1/databases/36b97ec3bd8f805f8648d25a06f6865c
{ "properties": {
    "Fix Prompt ù Lovable": { "name": "Fix Prompt — Lovable" },
    "Fix Prompt ù Bolt":    { "name": "Fix Prompt — Bolt" },
    "Fix Prompt ù Generic": { "name": "Fix Prompt — Generic" }
}}
```

Note: `findings_library/findings.csv` uses literal em-dashes (`—`) and that's the canonical version on disk. The Notion DB drifted on import.

### Notion — Outreach Tracker `Channel` is `rich_text` (template says `Select`)

`templates/notion/README.md` Outreach Tracker spec says `Channel | Select | Lovable Discord / Bolt Discord / Twitter / Reddit / Product Hunt / Other`. The live DB has `Channel` as `rich_text`. If you want to convert it, the Notion UI is the path (Notion API supports changing a property type but only between very limited pairs; rich_text→select usually requires deleting and recreating the column, which would discard the existing `EXAMPLE — delete me` row's value).

I would NOT run this without your sign-off. It's the kind of "you can't undo deleting a column" decision.

---

## What I tried but couldn't (blocked on something only you can do)

### Tally — API does not expose question/option mutation on this plan

Findings from `_probe_tally.py`:

- `GET https://api.tally.so/forms` returns one form: `QKOX1A` (PUBLISHED, 2 submissions, last updated 2026-05-25). The older `9qodVE` form referenced by `landing/assets/config.js` returns **HTTP 404 FORM_NOT_FOUND** from this API key's workspace. Either it lives in a different Tally account or it was deleted. **You should confirm the public URL `https://tally.so/r/9qodVE` still resolves in an incognito browser before any cold outreach.**
- `GET /forms/QKOX1A/questions` works (HTTP 200) and returns the 15-question schema.
- `GET /forms/QKOX1A/blocks` returns **HTTP 401 Unauthorized**. The blocks endpoint is the only one Tally documents for mutating question content; without 200 here, no API path exists to add `Webflow` to Q7, add `Pro Package ($99)` to Q8, or rewrite the Q9-Q11 conditional logic to fire for both Scale Up and Pro tiers.
- Recommendation: **Tally changes must happen in the UI.** All the new copy is in the four `docs/TALLY-*.txt` paste files — copy from there. The conditional-logic gotcha (Tally uses `/logic` blocks, not per-question menus) is documented at the top of `docs/ROB-REMAINING-TODO.md` §1.

### Google PageSpeed Insights API key — Google Cloud Console UI only

No alternate path. Generate at https://console.cloud.google.com → APIs & Services → Credentials → Create credentials.

### Plausible goal events — dashboard UI only

The Plausible REST API on the Hobby plan supports reading stats, not writing goals. Add the six goal events listed in `docs/ROB-REMAINING-TODO.md` (`FreeAuditSignup`, `StarterCheckout`, `ScaleUpCheckout`, `ProCheckout`, `IntakeFormStart`, `RescanAddOn`) in the Plausible dashboard.

### GitHub PAT — UI flow

The `gh` CLI is authenticated, but creating a new PAT scoped for the q19 GitHub-integration Pro-tier flow requires the github.com → Settings → Developer settings → Personal access tokens UI. Cannot be done headless.

### End-to-end payment test — real browser + real cards

Mission rule. Use incognito + a real card (your own), refund afterwards.

### DNS verification for `hello@launchlook.app`

**Already done.** Resend reports the `launchlook.app` domain as `status: "verified"`, `capabilities.sending: "enabled"`. No outstanding DNS records. The test email at the top of this file landed; if your inbox didn't see it, the issue is downstream of Resend (forwarding rule, spam filter).

---

## Open questions for Rob

1. **Currency.** USD-rebuild for Starter and Scale Up, or rename advertised prices to CAD? See "Stripe — currency mismatch" above.
2. **Handoff Report price.** $49 (now live) or $99 (matches landing copy)? Either way is one follow-up commit; I left the $49 link live per mission spec because doing nothing was the irreversible option. If you say "$99," I archive the $49 link before any buyer touches it.
3. **Tally form choice.** `QKOX1A` is the only form this API key can see; the `9qodVE` form referenced in `config.js` returns 404. Are they the same Tally account? If 9qodVE is dead, `landing/assets/config.js` `intakeFormUrl` needs to point at `https://tally.so/r/QKOX1A` (and you need to flip `QKOX1A` from DRAFT to PUBLISHED — already done per the probe, `status: PUBLISHED`).
4. **Notion schema migrations.** Three small mutations (add `Scale Up Package` + `Webflow`, rename three Findings columns) sit waiting. Approve in chat and I run them all in one turn.
5. **The $9 Saboteur link.** Per `docs/CONFIDENCE-CHECK-WORKFLOW.md` and the comment in `config.js`, the $9 within-14-days link is for Rob to send by post-delivery email, not a public landing CTA. It IS public (anyone with the URL can pay $9). That's by design per the workflow doc, but I want to confirm before someone scrapes it from the landing page bundle.

---

## Inventory captured during the run

### Stripe products — full snapshot

| Stripe Product ID | Name | Active | Currency | Created in this run |
|---|---|---|---|---|
| `prod_UaYWdSLpVvjkPh` | Verified Badge re-verify | yes | USD | **YES** (2026-05-26) |
| `prod_UaYWXAIIsuzkgO` | Handoff Report | yes | USD | **YES** (2026-05-26) |
| `prod_UaYWqXHpJ1PBS3` | Confidence Check (Saboteur re-scan) | yes | USD | **YES** (2026-05-26) |
| `prod_UaYW9iZCtYvqyw` | Pro Package | yes | USD | **YES** (2026-05-26) |
| `prod_UZ49fFi5Clxxgk` | LaunchLook Ship Package | yes | CAD | no (pre-existing) |
| `prod_UZ48FKGhAH3ANB` | LaunchLook Starter Package | yes | CAD | no (pre-existing) |
| `prod_UZ3Y5LErQavP7u` | LaunchLook Starter | yes | CAD | no (pre-existing duplicate; archive candidate) |
| `prod_UZ3d4CahdUQZUJ` | LaunchLook Ship | **INACTIVE** | CAD | no |

### Stripe prices — full snapshot

| Price ID | Product | Amount | Currency | Nickname | Active | Created in this run |
|---|---|---|---|---|---|---|
| `price_1TbNPABxCiPye3m0FGRoeUcN` | Verified Badge re-verify | 900 | USD | Verified Badge re-verify 9 USD | yes | **YES** |
| `price_1TbNP9BxCiPye3m0ttYEtpxO` | Handoff Report | 4900 | USD | Handoff Report 49 USD | yes | **YES** |
| `price_1TbNP7BxCiPye3m0QQBJMIAF` | Confidence Check | 900 | USD | Confidence Check 9 USD within 14 days | yes | **YES** |
| `price_1TbNP7BxCiPye3m07RzLHF2V` | Confidence Check | 1900 | USD | Confidence Check 19 USD standalone | yes | **YES** |
| `price_1TbNP5BxCiPye3m0Pn5T4zFJ` | Pro Package | 9900 | USD | Pro Package 99 USD | yes | **YES** |
| `price_1TZw1MBxCiPye3m02uW3Ww0s` | LaunchLook Ship Package | 2900 | CAD | (none) | yes | no |
| `price_1TZw0dBxCiPye3m0eLS4veNT` | LaunchLook Starter Package | 900 | CAD | (none) | yes | no |
| `price_1TZvW8BxCiPye3m0qFav7tUM` | LaunchLook Ship (inactive) | 2900 | CAD | (none) | yes | no |
| `price_1TZvRjBxCiPye3m0cYYpGtiY` | LaunchLook Starter | 900 | CAD | (none) | yes | no |

### Stripe Payment Links — post-run

| Payment Link ID | URL | Metadata | Created in this run | Wired in `config.js` slot |
|---|---|---|---|---|
| `plink_1TbNPABxCiPye3m0Q65r2HIS` | https://buy.stripe.com/00wfZgeB72jq9ZF3Cg3cc07 | `product=reverify` | **NEW** | `stripe.reverify` |
| `plink_1TbNP9BxCiPye3m0c5A1DNfq` | https://buy.stripe.com/3cIdR864B3nu7Rx4Gk3cc06 | `product=handoff_report` | **NEW** | `stripe.handoff` |
| `plink_1TbNP8BxCiPye3m0jKYev4V0` | https://buy.stripe.com/aFadR864BbU05JpdcQ3cc05 | `product=confidence_check`, `source=repeat` | **NEW** | `stripe.saboteurDiscounted` |
| `plink_1TbNP7BxCiPye3m0J6zs6w0N` | https://buy.stripe.com/3cI28q3Wt5vCb3J2yc3cc04 | `product=confidence_check`, `source=standalone` | **NEW** | `stripe.saboteur` |
| `plink_1TbNP6BxCiPye3m0IsakLVWj` | https://buy.stripe.com/9B600idx36zG3Bha0E3cc03 | `product=pro_package`, `tier=pro` | **NEW** | `stripe.pro` |
| `plink_1TZw1PBxCiPye3m0yYrf1RJC` | https://buy.stripe.com/cNi7sK3Wtgag9ZFc8M3cc02 | (none) | no | `stripe.scaleup` ($29 CAD — pre-bump) |
| `plink_1TZw0hBxCiPye3m03wX4DRU7` | https://buy.stripe.com/8x200i8cJ0bigo3fkY3cc01 | (none) | no | `stripe.starter` ($9 CAD — pre-bump) |
| `plink_1TZvtVBxCiPye3m0ympem2Z0` | (inactive) | (none) | no | n/a |

All five new Payment Links:
- `payment_method_types: null` (Stripe default — supports the full live payment method set for the account)
- `automatic_tax: { enabled: false }`
- `after_completion: { type: redirect, redirect.url: https://launchlook.app/thanks }`
- `customer_creation: "if_required"`, `billing_address_collection: "auto"`

### Resend domains

| Domain ID | Name | Status | Region | Sending | Receiving | Created |
|---|---|---|---|---|---|---|
| `54a4ef88-6142-4abf-acde-38f1fa0bafd4` | launchlook.app | **verified** | us-east-1 | enabled | disabled | 2026-05-25 23:02:41 UTC |

No DNS pending. The May 2026 test email (Message ID `6cea2df8-55ce-4b52-b7f9-2487f794f417`) was accepted by Resend with HTTP 200.

### Tally forms

| Form ID | Name | Status | Submissions | API access |
|---|---|---|---|---|
| `QKOX1A` | LaunchLook — Post-purchase intake | PUBLISHED | 2 | read OK (questions endpoint); **blocks endpoint returns 401**, no mutation path |
| `9qodVE` | (referenced in `config.js`) | **HTTP 404 from API** | unknown | not accessible via this API key |

Email notification target on `QKOX1A`: `romado33@gmail.com`. If you want it to also send to `hello@launchlook.app`, that's a UI-only change inside Tally.

### Notion DB schemas (live)

| DB | ID | Property count | Existing rows |
|---|---|---|---|
| Customers | `36b97ec3bd8f80668141d6083ca02385` | 22 | 7 (mix of real + example) |
| Findings Library | `36b97ec3bd8f805f8648d25a06f6865c` | 10 | 38 (FL-001 → FL-038) |
| Outreach Tracker | `36b97ec3bd8f8024a21ddeec7a995ae5` | 9 | 1 (`EXAMPLE — delete me`) |

Customers DB Tier options: `Full Package`, `Starter Package`, `Starter Package ($9)`, `Full Package ($29)`, `Pro Package`. **Missing: `Scale Up Package`.**

Customers DB Platform options: `Other`, `Cursor`, `V0`, `Replit`, `Base44`, `Bolt`, `Lovable`. **Missing: `Webflow`.**

Customers DB Status options: `Delivered`, `In Progress`, `Intake Received`, `Refunded`, `Paid`. Matches the constants used by `api/_lib/notion_helpers.py` (`STATUS_INTAKE = "Intake Received"`, `STATUS_PAID = "Paid"`).

Findings Library Severity options: `High`, `Medium`, `Critical`, `Low`. Three columns with corrupted em-dashes (see "Notion — Findings Library DB has corrupted column names" above).

Outreach Tracker properties: `Loom URL` (rich_text), `Paid` (checkbox), `Opened` (checkbox), `Notes` (rich_text), `Date Sent` (date), `Replied` (checkbox), `Channel` (rich_text — template says `select`), `App URL` (url), `Name` (title).

No example rows were seeded by this run. The Customers DB already has 7 rows (5 visible in the probe, mix of real `Rob` rows and `example` rows). Seeding more `[EXAMPLE]` rows would muddy that further. The migration calls above will make any future seeding cleaner.

---

## Files left at the repo root (untracked, safe to delete)

Probe and create scripts used during this run. Not committed. Delete after you've reviewed this file:

```
_probe_resend.py        _probe_resend.out         _probe_resend_output.txt
_probe_tally.py         _probe_tally.out          _probe_tally_output.txt
_probe_stripe.py        _probe_stripe.out         _probe_stripe_output.txt
_probe_notion.py        _probe_notion_output.txt
_probe_notion_rows.py   _probe_notion_rows.out
_create_stripe.py       stripe_created.json
_send_resend_test.py    _send_resend_test.out
```

Each is self-contained Python using only `urllib` + `.env`. None contains a secret — they all read `os.environ` at runtime.
