## Audit request

You are reviewing a single customer's live web app and drafting findings
for the {tier}. The cap is {max_findings} findings — see the tier
guidance below for the target range. Higher tiers expect more thorough
coverage, not just a ceiling.

### Customer

* Name: {customer_name}
* App: {app_name}
* URL: {app_url}
* Builder: {builder}
* Intake notes: {intake_notes}

### Tier guidance

{tier_guidance}

### Evidence you have

1. Screenshots: {n_screenshots} PNGs attached as vision inputs. Each is
   labelled `[viewport] /route`. Desktop is 1440x900. Mobile is 390x844.
   The mobile shot is what shows on a phone. The desktop shot is what
   shows on a laptop.

2. Prescreener regex hits: a list of pattern matches from
   findings_library/findings.csv. These are CANDIDATES, not confirmed
   findings. You decide whether to keep, merge, or reject each. False
   positives are expected. Pattern hits without screenshot or HTML
   evidence should be dropped.

3. Cleaned HTML extracts for each page that returned 200. Scripts and
   styles are stripped. You see visible text + key attributes (link
   hrefs, button text, alt text, meta tags). Routes that 404'd are also
   listed as evidence.

### Findings library context

The most common pre-launch issues are below as a reference. Use these
to ground your severity calibration and fix-prompt style. Do not feel
obligated to use one; do feel obligated to NOT invent a finding category
that doesn't match the library's spirit.

{findings_library}

### Prescreener hits

{prescreener_hits}

### HTML extracts

{html_extracts}

### Page status summary

{page_status_summary}

### Your task

Produce findings as a structured response, sorted by severity (critical,
high, medium, low). Hit the target range from the tier guidance above —
do not stop at the first handful of "obvious" issues if you're on Scale
Up or Pro. Each finding follows the schema defined by the tool /
response_format. Apply the voice, severity, and grounding rules from the
system prompt.

Coverage check before you return:
* Did you walk the findings library category by category? For Scale Up
  and Pro, every applicable category should be considered, not just the
  prescreener hits.
* Are you under the tier's target minimum? If yes, revisit the
  screenshots and HTML extracts — common misses are mobile breakpoint
  polish, accessibility (contrast, focus, alt text), copy quality,
  performance hints, SEO basics, trust signals, footer/legal pages,
  compliance basics (consent checkbox, order reference, unsubscribe link
  in transactional emails), and first-user orientation (is it obvious
  what the app does and what to do first?).
* You may NOT invent findings to pad the count. If a category truly does
  not apply, skip it. But the target minimum exists because most live
  apps genuinely have that many real issues — being well under usually
  means you stopped early.

Quality bar before you return:
* Did you quote at least one specific label / phrase / route per finding?
* Does title / what_we_saw / why_it_matters sound informal and human (Rob
  will ship this with light edits, not a full rewrite)?
* Is each fix_prompt a directive (not advice), and tailored to {builder}?
* Are there zero em-dashes in any field?
* Is every severity placement defensible against the system-prompt definitions?

Return the structured response only. No preamble.
